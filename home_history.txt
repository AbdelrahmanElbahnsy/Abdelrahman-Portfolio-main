commit d604b2fdb3239ba74c737157890e0d0a52b47fdb
Author: Abdelrahman Elbahnsy <abdelrahmanelbahnsy3@gmail.com>
Date:   Tue Sep 1 17:09:56 2026 +0300

    Backup before full cleanup

diff --git a/src/pages/Home.jsx b/src/pages/Home.jsx
index 6a7a398..8b69af7 100644
--- a/src/pages/Home.jsx
+++ b/src/pages/Home.jsx
@@ -1,6 +1,7 @@
 import React, { useEffect, useRef, lazy, Suspense } from 'react';
 import { useLanguage } from '../i18n/LanguageContext';
 import { useAppearance } from '../context/AppearanceContext';
+import { useVisitorPreferences } from '../context/VisitorPreferencesContext';
 import Navbar from '../components/layout/Navbar';
 import Footer from '../components/layout/Footer';
 import Hero from '../components/sections/Hero';
@@ -20,6 +21,7 @@ const Contact = lazy(() => import('../components/sections/Contact'));
 function Home({ splashDone = true }) {
   const { language } = useLanguage();
   const { activeSettings } = useAppearance();
+  const { effectiveTheme } = useVisitorPreferences();
   const hasTrackedVisitRef = useRef(false);
   const mainRef = useRef(null);
 
@@ -48,7 +50,7 @@ function Home({ splashDone = true }) {
     <div 
       className="page-wrapper min-h-screen flex flex-col relative portfolio-theme-root bg-[var(--theme-bg)] text-[var(--theme-text)] transition-colors duration-500" 
       dir={language === 'ar' ? 'rtl' : 'ltr'}
-      data-portfolio-theme={activeSettings?.theme || 'dark'}
+      data-portfolio-theme={effectiveTheme}
       style={themeStyle}
     >
       {/* Global Animated Background */}

commit ab7b35a39e1b0b640924a085dd393677a23b6ab1
Author: Abdelrahman Elbahnsy <abdelrahmanelbahnsy3@gmail.com>
Date:   Tue Sep 1 14:59:06 2026 +0300

    Backup before full cleanup

diff --git a/src/pages/Home.jsx b/src/pages/Home.jsx
index 046853c..6a7a398 100644
--- a/src/pages/Home.jsx
+++ b/src/pages/Home.jsx
@@ -1,5 +1,6 @@
 import React, { useEffect, useRef, lazy, Suspense } from 'react';
 import { useLanguage } from '../i18n/LanguageContext';
+import { useAppearance } from '../context/AppearanceContext';
 import Navbar from '../components/layout/Navbar';
 import Footer from '../components/layout/Footer';
 import Hero from '../components/sections/Hero';
@@ -18,6 +19,7 @@ const Contact = lazy(() => import('../components/sections/Contact'));
 
 function Home({ splashDone = true }) {
   const { language } = useLanguage();
+  const { activeSettings } = useAppearance();
   const hasTrackedVisitRef = useRef(false);
   const mainRef = useRef(null);
 
@@ -35,10 +37,19 @@ function Home({ splashDone = true }) {
     }
   }, []);
 
+  const themeStyle = activeSettings ? {
+    '--portfolio-primary': activeSettings.primaryColor,
+    '--portfolio-background': activeSettings.backgroundColor,
+    '--portfolio-surface': activeSettings.surfaceColor,
+    '--portfolio-text': activeSettings.textColor,
+  } : {};
+
   return (
     <div 
       className="page-wrapper min-h-screen flex flex-col relative portfolio-theme-root bg-[var(--theme-bg)] text-[var(--theme-text)] transition-colors duration-500" 
       dir={language === 'ar' ? 'rtl' : 'ltr'}
+      data-portfolio-theme={activeSettings?.theme || 'dark'}
+      style={themeStyle}
     >
       {/* Global Animated Background */}
       <AnimatedBackground />

commit 7e68a90f26bf2c64e237b4f2b87fabf3c40632d8
Author: Abdelrahman Elbahnsy <abdelrahmanelbahnsy3@gmail.com>
Date:   Mon Aug 31 18:27:19 2026 +0300

    Backup before full cleanup

diff --git a/src/pages/Home.jsx b/src/pages/Home.jsx
index d7cdb59..046853c 100644
--- a/src/pages/Home.jsx
+++ b/src/pages/Home.jsx
@@ -37,7 +37,7 @@ function Home({ splashDone = true }) {
 
   return (
     <div 
-      className="page-wrapper min-h-screen flex flex-col relative portfolio-theme-root" 
+      className="page-wrapper min-h-screen flex flex-col relative portfolio-theme-root bg-[var(--theme-bg)] text-[var(--theme-text)] transition-colors duration-500" 
       dir={language === 'ar' ? 'rtl' : 'ltr'}
     >
       {/* Global Animated Background */}
